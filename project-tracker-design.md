# 项目跟踪管理系统 — 设计文档（基于brainstorm-skills）

> **创建日期**: 2026-06-10  
> **状态**: 方案已确认，待开发

---

## 1. 背景与需求

### 现状
- 一个客户下有四个在建项目，分别由四人负责
- 每周五编写《信息化工作周报》，向公司及客户汇报
- 周报为腾讯文档格式，内容包括：各项目整体目标、本周工作、下周计划、风险提示
- 目前纯手工编辑，缺乏结构化管理和可视化跟踪

### 核心需求
1. 四人各自填写项目进度，数据统一存储
2. 四项目全局概览（一眼看全所有项目状态）
3. 单项目全局展示（时间线 + 详情）
4. 周报展示与趋势变化分析
5. 内部管理视角 + 客户汇报视角（同一数据源，不同呈现）
6. 不做甘特图等过于专业的管理工具，保持简洁直观

---

## 2. 方案选型

### 方案A：每周手工生成 HTML 页面 ❌

每周末汇总四人内容，写入 HTML 模板生成独立页面。

| 优点 | 缺点 |
|------|------|
| 上手快，第一周就能出页面 | 数据是死的，周与周无关联，无法做趋势分析 |
| 轻量，HTML 文件随处可打开 | 四人协作困难，格式统一靠自觉 |
| 布局样式完全自由 | 不可持续，文件越积越多越乱 |
| | 内部管理和客户汇报两个视角无法共用数据 |

> **结论：** 用文档当数据库，短期省事长期负债。不推荐。

### 方案B：完整 Web 项目跟踪管理系统 ✅

Web 应用 = 表单录入 + 数据结构化存储 + 多视角可视化展示。部署到 Vercel 免费托管。

| 优点 | 缺点 |
|------|------|
| 数据结构化存储，自动聚合，支持趋势分析 | 初始搭建需要一定开发工作 |
| 四人并行录入，数据自动汇总 | 有一定技术门槛（由 WorkBuddy 完成搭建） |
| 同一数据源，一键生成内部 + 客户两种视图 | 需注意避免功能过度设计，保持轻量 |
| Vercel 免费额度足够四人使用 | |
| 数据越积越多，分析价值越大 | |

> **结论：** 采用方案B。

---

## 3. 系统架构

```
┌─────────────────────────────────────┐
│            数据录入层                │
│  四人分别登录，通过 Web 表单填写进度  │
│  [负责人A] [负责人B] [负责人C] [负责人D] │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│            数据存储层                │
│  结构化 JSON 数据，按项目/周次存储    │
│  (Vercel KV 或文件存储)              │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│            可视化展示层              │
│  仪表盘 项目详情 周报时间线 趋势图表   │
│  热力看板 一键导出汇报               │
└──────┬──────────────┬───────────────┘
       │              │
       ▼              ▼
  [内部管理+领导]   [客户汇报]
```

### 技术栈
- **前端**: React 18 + React Router + Recharts（图表） + CSS Modules
- **数据存储**: Vercel KV（键值存储，免费 256MB）
- **API**: Vercel Functions（Serverless，免费 100GB-小时/月）
- **认证**: 简易密码登录（四人共享密码）
- **部署**: Vercel Hobby Plan（免费）

---

## 4. 页面结构

| 页面 | 路由 | 功能描述 | 面向受众 |
|------|------|---------|---------|
| 仪表盘 | `/` | 四个项目卡片总览 + 本周汇总统计 | 内部管理、领导 |
| 项目详情 | `/project/:id` | 单项目指标面板 + 周报时间线 + 趋势图 | 内部管理 |
| 周报编辑 | `/project/:id/report/new` | 结构化表单填写本周工作完成情况、下周计划、风险提示 | 项目负责人 |
| 客户汇报 | `/project/:id/report` | 一周报内容的整洁预览页，适合截图或导出给客户 | 客户 |

### 仪表盘 `/`
- 顶部导航栏（系统名称 + 当前周 + 用户信息）
- Tab 切换：项目总览 / 周报管理 / 趋势分析
- 四张项目卡片，每张显示：完成进度、周报数、风险项数、状态标签、负责人
- 底部本周汇总面板（完成事项数、计划事项数、累计风险、协作人数）

### 项目详情 `/project/:id`
- 项目头信息（名称、负责人、启动时间、状态标签）
- 关键指标横条（进度、累计周报数、风险项、完成/计划事项）
- Tab 切换：周报列表 / 进度趋势 / 客户汇报视图
- 周报列表：按时间倒序，每条显示周次、日期范围、完成/计划/风险统计、状态标签
- 进度趋势：折线图展示完成事项数随时间变化，风险项数量趋势

### 周报编辑 `/project/:id/report/new`
- 表单字段：
  - 周次和日期范围（自动生成或手动选择）
  - 整体目标及重点工作内容（文本区）
  - 本周工作完成情况（可添加多项，每项含：序号、事项名称、完成状态）
  - 下周工作计划（可添加多项）
  - 风险提示（可添加多项，每项含：序号、风险描述、风险等级：高/中/低）
- 提交后保存到数据存储，同时可在项目详情页查看

### 客户汇报 `/project/:id/report`
- 基于周报数据渲染的干净可打印页面
- 无编辑控件、无状态标签 — 纯信息展示
- 适合截图或导出为 PDF（浏览器打印）
- 可选：一键将本周周报数据写回腾讯文档

---

## 5. 数据模型

```typescript
// 项目
interface Project {
  id: string;
  name: string;           // 项目名称
  owner: string;          // 负责人
  startDate: string;      // 启动日期
  status: '正常推进' | '需关注' | '存在风险';
  color: string;          // 项目标识色
}

// 周报
interface WeeklyReport {
  id: string;
  projectId: string;
  weekLabel: string;      // 如 "第12周"
  weekStart: string;      // 周一日期
  weekEnd: string;        // 周五日期
  createdAt: string;
  updatedAt: string;
  summary: string;        // 整体概述
  completedItems: ReportItem[];
  plannedItems: ReportItem[];
  risks: Risk[];
}

// 事项（完成事项 / 计划事项）
interface ReportItem {
  id: string;
  order: number;          // 序号
  title: string;          // 事项名称
  detail?: string;        // 详细说明
}

// 风险提示
interface Risk {
  id: string;
  order: number;
  description: string;
  level: '高' | '中' | '低';
  status: '待处理' | '已解决' | '持续关注';
}
```

### 存储方案

Vercel KV 存储结构：

```
Key                         Value
project:{id}                JSON (Project)
report:{projectId}:{week}   JSON (WeeklyReport)
report:list:{projectId}     JSON (string[] of week labels)
projects:list               JSON (string[] of project IDs)
```

---

## 6. 开发计划

| 阶段 | 内容 | 产出 |
|------|------|------|
| 1. 基础搭建 | React 项目初始化、路由、布局框架 | 空架子可预览 |
| 2. 数据层 | 数据模型实现、Vercel KV 读写 API | CRUD 接口就绪 |
| 3. 仪表盘 | 项目总览页、项目卡片、汇总面板 | 首页可用 |
| 4. 项目详情 | 单项目页面、周报列表、趋势图 | 详情页可用 |
| 5. 周报编辑 | 表单页面、多事项添加/删除、提交保存 | 录入功能可用 |
| 6. 客户视图 | 汇报页面、打印样式、导出 | 客户侧可用 |
| 7. 登录认证 | 简易密码登录 | 权限就绪 |
| 8. 部署上线 | Vercel 配置、域名、部署验证 | 全功能上线 |

---

## 7. 注意事项

1. **保持轻量**：不引入数据库服务器，只用 KV 存储。四人项目不需要关系型数据库。
2. **不做甘特图**：保持界面直观，用卡片 + 时间线替代专业项目管理工具。
3. **客户视图要干净**：内部管理页面可以有状态标签、操作按钮；客户视图只展示信息，无任何 UI 杂音。
4. **数据可迁移**：JSON 结构存储，将来如需升级到数据库，直接导入即可。
5. **颜色方案**：每个项目分配一个主题色（蓝、绿、橙、紫），在全局视图中快速区分。
